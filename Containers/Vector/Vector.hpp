#ifndef X
#define X

#include <iostream>
#include <cstdlib>
#include <cmath>
#include <cassert>
#include <cstring>
#include <cstdio>
#include <cstdlib>
#include <cctype>
#include <ctime>
#include <climits>
#include <vector>
#include <string>
#include <stack>
#include <queue>
#include <deque>
#include <list>
#include <set>
#include <map>
#include <bitset>
#include <algorithm>
#include <memory>

template <class T, class Allocator = std::allocator<T> >
class Vector
{
    public:
        typedef T value_type;
        typedef Allocator allocator_type;
        typedef typename allocator_type::size_type size_type;
        typedef typename allocator_type::reference reference;
        typedef typename allocator_type::const_reference const_reference;
        typedef typename allocator_type::pointer pointer;
        typedef typename allocator_type::const_pointer const_pointer;
        typedef typename allocator_type::difference_type difference_type;
        typedef typename allocator_type::template rebind<value_type>::other rebind_type;
        typedef typename rebind_type::pointer rebind_pointer;
        typedef typename rebind_type::const_pointer rebind_const_pointer;
        typedef typename rebind_type::reference rebind_reference;
        typedef typename rebind_type::const_reference rebind_const_reference;
        typedef typename rebind_type::pointer rebind_pointer;
        typedef typename rebind_type::const_pointer rebind_const_pointer;
        typedef typename rebind_type::pointer rebind_pointer;
        typedef typename rebind_type::const_pointer rebind_const_pointer;
        typedef typename rebind_type::reference rebind_reference;
        typedef typename rebind_type::const_reference rebind_const_reference;
        typedef typename rebind_type::pointer rebind_pointer;
        typedef typename rebind_type::const_pointer rebind_const_pointer;
        typedef typename rebind_type::pointer rebind_pointer;
        typedef typename rebind_type::const_pointer rebind_const_pointer;
        typedef typename rebind_type::reference rebind_reference;
        typedef typename rebind_type::const_reference rebind_const_reference;
        typedef typename rebind_type::pointer rebind_pointer;
        typedef typename rebind_type::const_pointer rebind_const_pointer;
        typedef typename rebind_type::pointer rebind_pointer;

};


#endif